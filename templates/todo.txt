TODO
//- templates in einzelene dateien auslagern
// request an node server, welcher templates lädt und in json objekt packt und diese an den client schickt
//- client bereitet daten auf
//  - templates namen geben ( $.template(name, tmpl) )
//  - diese an dom anhängen
//  - daten liegen momentan noch auf client seite
//  - getTemplates function für jedes resource object
//- für jede person aus foaf ein extra resource object
- array data ajax
- listenelemente als template
- template property geben
- connect / express bsp für post http://stackoverflow.com/questions/5710358/how-to-get-post-query-in-express-node-js
- getTemplates funktion führt ajax anfrage an node server aus, diese templates werden in einem 
  cache gespeichert (evtl url oder andere parameter uebergeben)
- die vcard enthält bedingungen, die prüfen, ob die resource überhaupt die properties zur darstellung besitzt

-----
connect (bodyparser)
httpquery (node.js api)

- literal.widget (true false submit) überprüfen!!!!
